# Staging Environment Variables
project_id = "testpilotai-467409"

# Database passwords (you should change these to secure passwords)
database_password = "TestPilotDB2024!"
app_password = "TestPilotApp2024!"

# Optional: Path to service account key (if not using Application Default Credentials)
google_credentials_path = "../../terraform-admin-key.json" 